---
phase: 02-content-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
  - docs/overrides/main.html
  - docs/overrides/partials/announce.html
  - docs/_snippets/prereq-docker.md
  - docs/_snippets/api-base-url.md
  - docs/_snippets/standard-disclaimer.md
  - docs/_snippets.it/prereq-docker.md
  - docs/_snippets.it/api-base-url.md
  - docs/_snippets.it/standard-disclaimer.md
  - scripts/check-bilingual-pairs.sh
autonomous: true
requirements:
  - CNT-02
  - CNT-03
  - CNT-04
  - CNT-05

must_haves:
  truths:
    - "Announce bar renders Italian content on Italian pages and English content on English pages"
    - "Navigation labels display in Italian on Italian pages (all 24 labels: 8 sections + 16 child pages)"
    - "Admonition titles auto-translate to Italian (note->Nota, warning->Avviso, tip->Suggerimento, danger->Pericolo)"
    - "Snippet includes resolve correctly from docs/ directory"
    - "Language switcher stays on the same page when switching languages"
    - "Every .en.md file has a matching .it.md file (automated check passes)"
  artifacts:
    - path: "mkdocs.yml"
      provides: "Complete i18n configuration with custom_dir, snippets base_path, nav_translations, admonition_translations"
      contains: "custom_dir: docs/overrides"
    - path: "docs/overrides/main.html"
      provides: "i18n-aware announce bar template"
      contains: "i18n_current_language"
    - path: "docs/_snippets/prereq-docker.md"
      provides: "Docker prerequisite snippet (English)"
      contains: "Docker"
    - path: "docs/_snippets.it/prereq-docker.md"
      provides: "Docker prerequisite snippet (Italian)"
      contains: "Docker"
    - path: "scripts/check-bilingual-pairs.sh"
      provides: "Automated bilingual file pair verification"
      contains: "MISSING"
  key_links:
    - from: "docs/overrides/main.html"
      to: "mkdocs.yml theme.custom_dir"
      via: "template override mechanism"
      pattern: "custom_dir: docs/overrides"
    - from: "mkdocs.yml plugins.i18n.languages"
      to: "Italian nav labels"
      via: "nav_translations configuration"
      pattern: "nav_translations"
    - from: "docs/_snippets/prereq-docker.md"
      to: "mkdocs.yml pymdownx.snippets"
      via: "base_path: docs resolution"
      pattern: "base_path"
---

<objective>
Fix MkDocs configuration gaps and create the infrastructure for bilingual content rendering.

Purpose: Phase 1 left several configuration issues that prevent i18n features from working correctly: the announce bar is not rendering (missing custom_dir), nav_translations are incomplete (only 8 section-level labels), extra.alternate is redundant, snippet base_path is unconfigured, and admonition_translations for Italian are missing. This plan fixes all configuration gaps and creates the shared snippet system that content pages will use.

Output: Updated mkdocs.yml with complete i18n config, i18n-aware announce bar template, shared snippet directories with 6 snippet files, and a bilingual pairs validation script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-i18n/02-CONTEXT.md
@.planning/phases/02-content-i18n/02-RESEARCH.md
@.planning/phases/02-content-i18n/02-PATTERNS.md
@.planning/phases/02-content-i18n/02-UI-SPEC.md
@.planning/phases/02-content-i18n/02-VALIDATION.md
@mkdocs.yml
@docs/overrides/partials/announce.html

<interfaces>
From mkdocs.yml (current config, lines to modify):
```yaml
theme:
  name: material
  language: en  # Line 24 -- need custom_dir after this

# Line 98:
  - pymdownx.snippets                # Need base_path + check_paths

# Lines 71-79:
          nav_translations:
            Setup: Configurazione      # Need: change to "Installazione e configurazione" + add 16 child-page labels

# Lines 153-160:
extra:
  alternate:                          # Need: remove this entire block
    - name: English
      link: /en/
      lang: en
    - name: Italiano
      link: /it/
      lang: it
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mkdocs.yml configuration for i18n completeness</name>
  <files>mkdocs.yml</files>
  <read_first>mkdocs.yml</read_first>
  <action>
Make five targeted modifications to mkdocs.yml. Read the full file first, then apply all changes:

**Modification 1 -- Add custom_dir to theme config (after line 23):**
Add `custom_dir: docs/overrides` between `name: material` and `language: en`. The theme block becomes:
```yaml
theme:
  name: material
  custom_dir: docs/overrides    # Enable template overrides for announce bar
  language: en
```
This is CRITICAL -- without custom_dir, no template override in docs/overrides/ will render (Pitfall 1 in RESEARCH.md).

**Modification 2 -- Configure pymdownx.snippets with base_path (replace line 98):**
Replace the bare `- pymdownx.snippets` entry with:
```yaml
  - pymdownx.snippets:
      base_path: docs            # Resolve snippet paths relative to docs/
      check_paths: true          # Warn if snippet file not found
```
Without base_path, snippet includes like `--8<-- "_snippets/file.md"` would resolve to the project root instead of docs/ (Pitfall 5).

**Modification 3 -- Expand nav_translations and fix Setup collision (replace lines 71-79):**
Replace the existing nav_translations block (which has only 8 section-level labels) with the complete set of 24 labels (8 section + 16 child-page). Also fix the "Setup" translation from "Configurazione" to "Installazione e configurazione" to avoid collision with the "Configuration" child page (Pitfall 2):
```yaml
          nav_translations:
            # Section-level (8 items)
            Setup: "Installazione e configurazione"
            Chat: Chat
            "Documents & RAG": "Documenti e RAG"
            "Workspaces & Projects": "Aree di lavoro e Progetti"
            Administration: Amministrazione
            API: API
            Deployment: Distribuzione
            Security: Sicurezza
            # Child-page level (16 items -- new)
            Home: Home
            Installation: Installazione
            Configuration: Configurazione
            "Getting Started": "Per iniziare"
            Features: Funzionalita
            Uploading: Caricamento
            Querying: Interrogazione
            Workspaces: "Aree di lavoro"
            Projects: Progetti
            RBAC: RBAC
            Settings: Impostazioni
            Endpoints: Endpoint
            Authentication: Autenticazione
            Docker: Docker
            "Air-Gap Setup": "Configurazione Air-Gap"
            Overview: Panoramica
            Compliance: Conformita
```

**Modification 4 -- Remove extra.alternate block (remove lines 153-160):**
Remove the entire `extra:` -> `alternate:` block. The `reconfigure_material: true` in the i18n plugin already auto-generates per-page language alternates with stay-on-page behavior (Pitfall 4). The manual entries are redundant and could mislead future editors. If `extra:` has no other keys after removing alternate, remove the entire `extra:` block.

**Modification 5 -- Add admonition_translations to Italian locale (after nav_translations block):**
Add the following after the nav_translations block under the Italian locale config:
```yaml
          admonition_translations:
            note: Nota
            warning: Avviso
            tip: Suggerimento
            danger: Pericolo
```
This auto-translates admonition type names when no explicit title is given in the markdown (per D-08).

**Important:** Do NOT change any other lines in mkdocs.yml. Keep all comments, spacing, and other configuration intact.
  </action>
  <verify>
    <automated>cd /home/simos/progs/Elia/simos-chat-docs && mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <done>
    - mkdocs.yml contains `custom_dir: docs/overrides` under theme:
    - mkdocs.yml contains `base_path: docs` and `check_paths: true` under pymdownx.snippets
    - mkdocs.yml nav_translations has 24 entries (8 section + 16 child-page labels)
    - mkdocs.yml nav_translations has Setup: "Installazione e configurazione" (not "Configurazione")
    - mkdocs.yml has admonition_translations with note, warning, tip, danger for Italian locale
    - mkdocs.yml does NOT contain `alternate:` under `extra:`
    - `mkdocs build --strict` exits with code 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create announce bar template, snippet files, and validation script</name>
  <files>docs/overrides/main.html, docs/overrides/partials/announce.html, docs/_snippets/prereq-docker.md, docs/_snippets/api-base-url.md, docs/_snippets/standard-disclaimer.md, docs/_snippets.it/prereq-docker.md, docs/_snippets.it/api-base-url.md, docs/_snippets.it/standard-disclaimer.md, scripts/check-bilingual-pairs.sh</files>
  <read_first>docs/overrides/partials/announce.html</read_first>
  <action>
**Step 1: Delete the old announce bar template.**
Remove `docs/overrides/partials/announce.html`. This file uses the wrong override pattern (partial include instead of block override). Material theme's announce bar is a `{% block announce %}` in base.html, not a partial include. The file must be deleted to avoid confusion.

**Step 2: Create the i18n-aware announce bar template.**
Create `docs/overrides/main.html` with the following exact content:
```html
{% extends "base.html" %}

{% block announce %}
  {% if i18n_current_language == "it" %}
  <span>
    <a href="/it/chat/getting-started/">
      Documentazione v1 &mdash; Inizia con AirGap AI Chatbot
    </a>
  </span>
  {% else %}
  <span>
    <a href="/chat/getting-started/">
      v1 Documentation &mdash; Getting started with AirGap AI Chatbot
    </a>
  </span>
  {% endif %}
{% endblock %}
```
Uses absolute paths (/it/... and /chat/...) because the announce bar appears on pages at different depths. Uses `i18n_current_language` variable exposed by mkdocs-static-i18n (per D-07).

**Step 3: Create snippet directories and English snippets.**
Create `docs/_snippets/` directory (if not exists). Create three English snippet files:

`docs/_snippets/prereq-docker.md`:
```markdown
!!! note "Prerequisites"

    - Docker 20.10+ installed and running
    - Docker Compose v2+ (included with Docker Desktop)
    - At least 4 GB of RAM available

??? tip "Verify Docker installation"

    ```bash
    docker --version
    docker compose version
    ```
```

`docs/_snippets/api-base-url.md`:
```markdown
The API is available at:

```bash
BASE_URL="http://localhost:3000/api"
```
```

`docs/_snippets/standard-disclaimer.md`:
```markdown
!!! warning "Self-Hosted Environment"

    This guide assumes you are running AirGap AI Chatbot in a self-hosted environment. Adjust URLs and credentials for your deployment.
```

**Step 4: Create Italian snippet directory and Italian snippets.**
Create `docs/_snippets.it/` directory (if not exists). Create three Italian snippet files that are structural mirrors of the English versions (per D-05), with technical terms kept in English (per D-06):

`docs/_snippets.it/prereq-docker.md`:
```markdown
!!! note "Prerequisiti"

    - Docker 20.10+ installato e in esecuzione
    - Docker Compose v2+ (incluso con Docker Desktop)
    - Almeno 4 GB di RAM disponibili

??? tip "Verifica installazione Docker"

    ```bash
    docker --version
    docker compose version
    ```
```

`docs/_snippets.it/api-base-url.md`:
```markdown
L'API e disponibile all'indirizzo:

```bash
BASE_URL="http://localhost:3000/api"
```
```

`docs/_snippets.it/standard-disclaimer.md`:
```markdown
!!! warning "Ambiente Self-Hosted"

    Questa guida presuppone che AirGap AI Chatbot sia in esecuzione in un ambiente self-hosted. Adattare gli URL e le credenziali per il proprio deployment.
```

Note: Admonition type keywords (`!!! note`, `!!! warning`, `!!! tip`, `!!! danger`) stay in English in Italian snippets. The `admonition_translations` config in mkdocs.yml auto-translates the titles. Only the explicit title strings (in quotes) are in Italian.

**Step 5: Create the bilingual pairs validation script.**
Create `scripts/` directory (if not exists). Create `scripts/check-bilingual-pairs.sh` with the following content:

```bash
#!/usr/bin/env bash
# check-bilingual-pairs.sh -- Verify every .en.md has a matching .it.md
set -euo pipefail

DOCS_DIR="${1:-docs}"
MISSING=0

echo "Checking bilingual file pairs in ${DOCS_DIR}..."

while IFS= read -r en_file; do
  it_file="${en_file%.en.md}.it.md"
  if [[ ! -f "$it_file" ]]; then
    echo "MISSING: $it_file (pair for $en_file)"
    MISSING=$((MISSING + 1))
  fi
done < <(find "${DOCS_DIR}" -name "*.en.md" -not -path "*/_snippets*")

while IFS= read -r it_file; do
  en_file="${it_file%.it.md}.en.md"
  if [[ ! -f "$en_file" ]]; then
    echo "MISSING: $en_file (pair for $it_file)"
    MISSING=$((MISSING + 1))
  fi
done < <(find "${DOCS_DIR}" -name "*.it.md" -not -path "*/_snippets*")

if [[ $MISSING -gt 0 ]]; then
  echo "FAIL: ${MISSING} missing pair(s)"
  exit 1
else
  echo "PASS: All bilingual pairs complete"
  exit 0
fi
```

Make the script executable: `chmod +x scripts/check-bilingual-pairs.sh`
  </action>
  <verify>
    <automated>cd /home/simos/progs/Elia/simos-chat-docs && test -f docs/overrides/main.html && test ! -f docs/overrides/partials/announce.html && test -f docs/_snippets/prereq-docker.md && test -f docs/_snippets.it/prereq-docker.md && bash scripts/check-bilingual-pairs.sh && mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <done>
    - docs/overrides/main.html exists with i18n_current_language conditional and absolute path links
    - docs/overrides/partials/announce.html is deleted
    - docs/_snippets/ contains 3 English snippet files (prereq-docker.md, api-base-url.md, standard-disclaimer.md)
    - docs/_snippets.it/ contains 3 Italian snippet files (matching pairs)
    - scripts/check-bilingual-pairs.sh is executable and reports PASS for all existing pairs
    - mkdocs build --strict exits with code 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time config -> Static site | mkdocs.yml and templates are trusted build inputs; no untrusted data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | mkdocs.yml | accept | Static config file, no user input |
| T-02-02 | I | Announce bar links | accept | Links point to internal pages only |
| T-02-03 | S | Snippet includes | accept | Snippets are version-controlled files, no dynamic input |
</threat_model>

<verification>
1. `mkdocs build --strict` exits with code 0
2. `bash scripts/check-bilingual-pairs.sh` reports PASS
3. `grep -c "custom_dir" mkdocs.yml` returns 1
4. `grep -c "base_path: docs" mkdocs.yml` returns 1
5. `grep -c "admonition_translations" mkdocs.yml` returns 1
6. `grep -c "Installazione e configurazione" mkdocs.yml` returns 1
7. `test -f docs/overrides/main.html` succeeds
8. `test ! -f docs/overrides/partials/announce.html` succeeds
</verification>

<success_criteria>
- All 5 mkdocs.yml modifications are applied and `mkdocs build --strict` passes
- Announce bar template uses i18n_current_language conditional with absolute paths per D-07
- Shared snippet directories exist with 3 English and 3 Italian snippet files
- Validation script confirms all existing bilingual pairs are complete
- nav_translations has 24 entries (8 section + 16 child-page)
- extra.alternate block removed from mkdocs.yml
- admonition_translations configured for Italian locale
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-i18n/02-01-SUMMARY.md`
</output>