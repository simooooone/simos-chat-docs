---
phase: 03-online-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - mkdocs.yml
autonomous: false
requirements:
  - DPL-01
  - DPL-04

user_setup:
  - service: GitHub
    why: "Netlify git integration requires a GitHub repository"
    env_vars: []
    dashboard_config:
      - task: "Create GitHub repository (if not exists)"
        location: "https://github.com/new"
      - task: "Set main as default branch"
        location: "Repository Settings > Default branch"
  - service: Netlify
    why: "Static site hosting with git integration for automatic deploys"
    env_vars: []
    dashboard_config:
      - task: "Create new site and connect to GitHub repo"
        location: "https://app.netlify.com/start"
      - task: "Set production branch to main"
        location: "Site settings > Build and deploy > Continuous Deployment"
      - task: "Verify deploy previews are enabled"
        location: "Site settings > Build and deploy > Deploy previews"

must_haves:
  truths:
    - "Pushing to the main branch triggers an automatic deployment to Netlify"
    - "The site is accessible at the Netlify subdomain URL"
    - "Pull requests generate deploy preview URLs commented by Netlify bot"
    - "Deploy previews are cleaned up when PRs are closed or merged"
    - "Security headers appear in responses from the live site"
  artifacts:
    - path: "mkdocs.yml"
      provides: "Repo URL pointing to actual GitHub repository"
      contains: "repo_url:"
    - path: "Netlify site"
      provides: "Live deployment at docs-airgap-chatbot.netlify.app"
      contains: ""
  key_links:
    - from: "GitHub repo (main branch)"
      to: "Netlify build system"
      via: "Netlify git integration (auto-trigger on push)"
      pattern: "push to main"
    - from: "GitHub repo (PR branch)"
      to: "Netlify deploy preview"
      via: "Netlify git integration (auto-trigger on PR)"
      pattern: "deploy-preview-"
    - from: "Netlify build system"
      to: "site/ output"
      via: "mkdocs build command from netlify.toml"
      pattern: "mkdocs build"
---

<objective>
Verify the complete deployment pipeline works end-to-end: GitHub repo connected to Netlify, production deploys on push to main, deploy previews on PRs, and security headers served correctly.

Purpose: Plan 01 created the configuration files; this plan validates they work in the actual Netlify environment and completes the user-facing deployment goals (DPL-01 production deploy, DPL-04 deploy previews).

Output: Live Netlify site with verified CI/CD pipeline
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-online-deployment/03-CONTEXT.md
@.planning/phases/03-online-deployment/03-RESEARCH.md
@.planning/phases/03-online-deployment/03-01-SUMMARY.md

<interfaces>
<!-- Key outputs from Plan 01 that this plan depends on -->

From netlify.toml (created in Plan 01):
```toml
[build]
  command = "mkdocs build"
  publish = "site/"
```

From runtime.txt (created in Plan 01):
```
3.12
```

From .github/workflows/build-check.yml (created in Plan 01):
```yaml
name: Build Check
on:
  pull_request:
    branches: [main]
```

From mkdocs.yml (updated in Plan 01):
```yaml
site_url: https://docs-airgap-chatbot.netlify.app/
```

Current mkdocs.yml repo_url (line 17):
```yaml
repo_url: https://github.com/elia/simos-chat-docs
```
This may need updating to match the actual GitHub repo URL.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify local build and push to GitHub</name>
  <files>mkdocs.yml</files>
  <read_first>
    mkdocs.yml
    netlify.toml
    runtime.txt
    .github/workflows/build-check.yml
  </read_first>
  <action>1. **Run `mkdocs build --strict`** locally to verify all config files from Plan 01 produce a valid build with zero warnings.

2. **Check if a git remote exists** by running `git remote -v`. If no remote is configured:
   - Use `gh repo create elia/simos-chat-docs --public --source=. --push` to create a GitHub repo and push the current code. The `--source=.` flag uses the current directory; `--push` pushes all commits; `--public` makes it a public repo (appropriate for public documentation).
   - After creating, verify the remote was added with `git remote -v`.

3. **If a remote already exists**, verify it points to the correct repository. If the `repo_url` in mkdocs.yml does not match the actual remote URL, update `repo_url` (currently line 17: `repo_url: https://github.com/elia/simos-chat-docs`) to match the actual GitHub URL.

4. **Push the current branch** to the remote main branch if not already pushed: `git push -u origin master` or `git push -u origin main` (whichever is the current default branch). If the local branch is `master` but Netlify expects `main`, rename it: `git branch -M main && git push -u origin main`.

5. **Verify** that the remote main branch is up to date: `git log --oneline origin/main..HEAD` should return empty (no unpushed commits).
</action>
  <verify>
    <automated>cd /home/simos/progs/Elia/simos-chat-docs && mkdocs build --strict 2>&1 | tail -5 && git remote -v 2>/dev/null | grep -q 'origin' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `mkdocs build --strict` exits 0 with no warnings
    - `git remote -v` shows an `origin` remote pointing to a GitHub repository
    - All commits are pushed to the remote main branch (`git log --oneline origin/main..HEAD` is empty)
    - If `repo_url` was updated, mkdocs.yml `repo_url` matches the actual GitHub URL
  </acceptance_criteria>
  <done>Local build passes with --strict, code is pushed to GitHub main branch</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Create Netlify site and verify production deploy</name>
  <files>netlify.toml, runtime.txt, .github/workflows/build-check.yml, mkdocs.yml</files>
  <read_first>
    netlify.toml
    .planning/phases/03-online-deployment/03-RESEARCH.md
  </read_first>
  <what-built>Deployment configuration files (netlify.toml, runtime.txt, build-check.yml) and the full codebase are pushed to GitHub. The Netlify site needs to be created and connected.</what-built>
  <how-to-verify>
    1. Visit the Netlify subdomain URL (e.g., https://docs-airgap-chatbot.netlify.app/) -- homepage should load with English documentation
    2. Click around the site -- navigation, search, language switcher should all work
    3. Visit `/it/` -- Italian version should load
    4. Visit `/en/` -- should redirect to root `/`
    5. Open browser dev tools > Network > check response headers on any page request
    6. Verify the following headers appear: X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Content-Security-Policy, Strict-Transport-Security
  </how-to-verify>
  <action>The human must perform Netlify setup steps that cannot be automated.

**Steps to complete:**

1. Go to https://app.netlify.com/start (or log in to Netlify and click "Add new site" > "Import an existing project")

2. Connect to the GitHub repository:
   - Select "GitHub" as the Git provider
   - Authorize Netlify to access the repository (if prompted)
   - Select the `elia/simos-chat-docs` repository

3. Configure build settings (Netlify should auto-detect these from `netlify.toml`, but verify):
   - **Branch to deploy:** `main` (CRITICAL -- must be `main`, not `master`; see RESEARCH.md Pitfall 5)
   - **Build command:** `mkdocs build` (should be read from netlify.toml)
   - **Publish directory:** `site/` (should be read from netlify.toml)

4. Click "Deploy site" and wait for the first deploy to complete

5. After the first deploy completes, verify:
   - The site is accessible at the Netlify subdomain URL (e.g., https://docs-airgap-chatbot.netlify.app/)
   - The homepage loads with English content (English is at root `/`)
   - Italian content loads at `/it/`
   - Navigate to `/en/` -- it should redirect to `/` (the /en/* redirect rule)
   - Security headers are present (open browser dev tools > Network tab > refresh > click the document request > check Response Headers for X-Frame-Options, X-Content-Type-Options, etc.)
   - HTTPS is active (padlock icon in browser address bar, D-12)
</action>
  <verify>
    <automated>MISSING -- manual verification required: site must be live and accessible at Netlify URL</automated>
  </verify>
  <acceptance_criteria>
    - Site loads at Netlify subdomain URL with English content at root `/`
    - Italian content loads at `/it/`
    - `/en/` redirects to `/` (301)
    - Response headers include: X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Content-Security-Policy, Strict-Transport-Security
    - HTTPS is active (padlock icon)
  </acceptance_criteria>
  <done>Netlify site is live, homepage loads, /it/ loads Italian, /en/ redirects to /, security headers present, HTTPS active</done>
  <resume-signal>Type "approved" if the site is live and working, or describe any issues encountered</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify deploy previews and GitHub Actions build-check</name>
  <files>.github/workflows/build-check.yml, docs/index.en.md</files>
  <read_first>
    .github/workflows/build-check.yml
    .planning/phases/03-online-deployment/03-RESEARCH.md
  </read_first>
  <what-built>Netlify site is live with production deploys working. Now need to verify the PR workflow: deploy previews and the GitHub Actions build-check.</what-built>
  <how-to-verify>
    1. Open PR created on `test/deploy-preview` branch
    2. GitHub Actions "Build Check" appears on the PR and passes (green checkmark)
    3. Netlify bot comments on the PR with a deploy preview URL
    4. Deploy preview URL loads the site with the test change
    5. After closing the PR, deploy preview is eventually cleaned up (D-06)
  </how-to-verify>
  <action>The human must create a test PR to verify the full PR workflow.

**Steps to complete:**

1. Create a test branch and a trivial change (e.g., add a comment to a documentation file):
   ```bash
   git checkout -b test/deploy-preview
   echo "<!-- test comment -->" >> docs/index.en.md
   git add docs/index.en.md
   git commit -m "test: verify deploy preview workflow"
   git push -u origin test/deploy-preview
   ```

2. Open a pull request from `test/deploy-preview` to `main` using `gh pr create --title "Test: Deploy preview verification" --body "Testing deploy preview and build-check workflow"`

3. Wait for two automated checks to appear on the PR:
   - **GitHub Actions build-check:** Should appear within 1-2 minutes. It should run `mkdocs build --strict` and pass (green checkmark).
   - **Netlify deploy preview:** Should appear within 2-3 minutes. Netlify bot should comment on the PR with a deploy preview URL.

4. Verify the deploy preview:
   - Click the deploy preview URL from the Netlify bot comment
   - The preview site should load and reflect the test change
   - The preview URL format should be like `deploy-preview-NN--docs-airgap-chatbot.netlify.app`

5. Verify the GitHub Actions check:
   - Go to the PR's "Checks" tab
   - The "Build Check" workflow should show as passed (green)

6. Close the test PR without merging:
   ```bash
   gh pr close test/deploy-preview
   ```

7. Verify deploy preview cleanup (D-06):
   - After the PR is closed, the deploy preview URL should eventually return 404 (may take a few minutes)

8. Clean up the test branch:
   ```bash
   git push origin --delete test/deploy-preview
   git checkout main
   git branch -d test/deploy-preview
   ```
</action>
  <verify>
    <automated>MISSING -- manual verification required: PR must trigger GitHub Actions check and Netlify deploy preview</automated>
  </verify>
  <acceptance_criteria>
    - GitHub Actions "Build Check" workflow runs on the test PR and passes (green checkmark)
    - Netlify bot comments on the PR with a deploy preview URL
    - Deploy preview URL loads the site
    - After closing the PR, deploy preview is cleaned up
  </acceptance_criteria>
  <done>PR triggers GitHub Actions build-check (pass) and Netlify deploy preview; deploy preview cleaned up on PR close</done>
  <resume-signal>Type "approved" if deploy preview and build-check both work, or describe any issues encountered</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub -> Netlify | Netlify has read access to the GitHub repo via OAuth; pushes to main trigger builds |
| Public internet -> Netlify CDN | Already covered in Plan 01 threat model; verified here via live headers check |
| PR author -> Deploy preview | Anyone who opens a PR gets a deploy preview; acceptable per D-04 (public previews) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | Malicious PR deploy preview | accept | D-04 locks public previews; previews are on separate subdomain, do not affect production; appropriate for public documentation |
| T-03-08 | Information Disclosure | Build logs visible on Netlify | accept | Public repo; build logs may show dependency versions but contain no secrets; low-value target |
</threat_model>

<verification>
1. Site accessible at Netlify subdomain URL
2. English content loads at `/`, Italian at `/it/`
3. `/en/` redirects to `/` (301)
4. Security headers present in live responses
5. HTTPS active (Let's Encrypt, D-12)
6. PR triggers GitHub Actions build-check (pass)
7. PR triggers Netlify deploy preview (URL in bot comment)
8. Closed PR deploy preview cleaned up (D-06)
</verification>

<success_criteria>
- Documentation site is live at the Netlify subdomain with HTTPS
- Push to main triggers automatic production deploy (DPL-01)
- PR triggers deploy preview with Netlify bot comment (DPL-04)
- PR triggers GitHub Actions build-check that passes (DPL-03)
- Security headers appear in live responses (D-09)
- Deploy preview cleanup works on PR close (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/03-online-deployment/03-02-SUMMARY.md`
</output>